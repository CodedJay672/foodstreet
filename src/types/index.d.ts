import { URL } from "url";

type ShopType = {
  creator: string;
  name: string;
  email: string;
  location: string;
  description: string;
  phone: string;
  occupation: string;
  "work-address": string;
};
